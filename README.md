# e-coomers
Frontend folder manages the UI using React, while the Backend folder handles database connectivity and queries using Node.js and Express.js. Run npm install, then npm start, and import the database for proper functionality.
